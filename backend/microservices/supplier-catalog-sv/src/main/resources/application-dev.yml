spring:
  datasource:
    url: jdbc:postgresql://localhost:5544/supplier_catalog_db
    username: retailmanager
    password: RetailMgr2024!

  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update

logging:
  level:
    root: INFO
    org.springframework: INFO
    com.retailmanager: DEBUG
    org.hibernate.SQL: DEBUG
